"""
AIAssistant节点模块
包含LLM相关的节点实现
"""

import os
import sys
import logging

# 配置日志
logger = logging.getLogger('axun.ai_assistant')

# 获取当前模块所在目录
CURRENT_DIR = os.path.dirname(os.path.abspath(__file__))

# 将当前目录添加到系统路径
if CURRENT_DIR not in sys.path:
    sys.path.append(CURRENT_DIR)

# 导入节点类
from .llm_node import LLMNode

# 节点类映射
NODE_CLASS_MAPPINGS = {
    "LLMNode": LLMNode
}

# 节点显示名称映射
NODE_DISPLAY_NAME_MAPPINGS = {
    "LLMNode": "LLM Assistant 🤖"
}

# Web API路由
WEB_ROUTES = [
    # 如果需要添加Web路由，在这里添加
]

__all__ = ['NODE_CLASS_MAPPINGS', 'NODE_DISPLAY_NAME_MAPPINGS', 'WEB_ROUTES'] 